{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @sneaker-drop/db build && pnpm --filter @sneaker-drop/api build && pnpm --filter @sneaker-drop/client build",
  "outputDirectory": "apps/client/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/[...route]"
    }
  ],
  "functions": {
    "api/[...route].ts": {
      "maxDuration": 30
    }
  }
}
